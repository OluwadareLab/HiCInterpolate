Task:
    - Preserve current architecture.
    - do not touch or edit unnecessary files.
    - think deeply, say less in context output.
    - Add noise preservation block
       Implement a custom PyTorch/TensorFlow module for temporal interpolation between two structured frame inputs ($I_0$ and $I_1$) using morphological noise segregation and a custom Conv U-Net flow prediction block equipped with a PWC-Net cost volume.Architectural Constraints & Pipeline:Morphological Noise Segregation Layer:Define a fixed/trainable morphological opening layer ($I \circ S$) using a structural element kernel size of $3 \times 3$ or $5 \times 5$.Segregate each input image into two streams:Structure Component ($I_{struct}$): The output of the morphological opening.Noise Component ($I_{noise}$): The absolute difference map ($I_{noise} = |I - I_{struct}|$).PWC-Net Feature Extraction & Cost Volume Block:Pass both $I_{0, struct}$ and $I_{1, struct}$ through a multi-scale convolutional feature extractor to obtain pyramid features at scale $l$.Compute a partial PWC-Net cost volume by measuring the pixel-by-pixel correlation between the feature representation of $I_{0, struct}$ and the warped feature representation of $I_{1, struct}$ at scale $l$.Conv U-Net Flow Prediction (No Residual Skip Connections):Input the calculated cost volume into a custom Convolutional U-Net to predict the intermediate bi-directional optical flow fields ($F_{t \to 0}$ and $F_{t \to 1}$).Strict Restriction: Do not use standard additive residual skip connections or standard concatenations from encoder to decoder layers. Instead, use a Bottleneck Compression Flow: pass latent representations strictly through sequential downsampling/upsampling layers to force the decoder to reconstruct features solely based on the compressed cost-volume representations.Temporal Interpolation & Decoder Mask Blending:Warp the structure maps ($I_{0, struct}, I_{1, struct}$) and the segregated noise maps ($I_{0, noise}, I_{1, noise}$) to target timestamp $t$ using backward warping layers powered by the predicted flows.Feed the warped structures and cost volume latents into the final Decoder stage to reconstruct the primary structural frame $\hat{I}_{t, struct}$.The Decoder must output a dynamic blending mask $M_t$. Combine the warped noise maps via a temporal linear interpolation ($(1-t) \cdot I_{0, noise}^{warped} + t \cdot I_{1, noise}^{warped}$) to create a unified noise field $\hat{I}_{t, noise}$.Final Output Synthesis: Formulate the final reconstructed tensor uniquely by blending the decoder's structural output and the synthesized noise using the generated mask:$$\hat{I}_t = M_t \odot \hat{I}_{t, struct} + (1 - M_t) \odot \hat{I}_{t, noise}$$Write clean, modular code implementing this pipeline, ensuring the absence of shortcut connections in the U-Net block. Flow Prediction model is already there, if you want to reduce work, reuse it or write a same conceptual block for this task, if it is not good for this task, write your own. no deterministic oepration, it should be a total and pure learnable block, it will learn to interpolate temporal noise. it will uniquily learn and work on top of the current architecture independetly.
